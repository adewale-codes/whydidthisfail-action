{
  "name": "whydidthisfail-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: automatically diagnoses a failed CI job and posts the result as a comment.",
  "main": "src/index.js",
  "scripts": {
    "test": "node test/simulate.js"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/github": "^8.0.1"
  }
}
